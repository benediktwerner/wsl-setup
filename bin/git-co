#!/bin/sh

confirm() {
    read -p "$1" -rn 1
    [[ "$?" == "0" ]] && [[ "$REPLY" == "" ]]
}

git checkout $@

if (( $? ))
then
    if confirm "No branch named '$1', press ENTER to create it"
    then
        git checkout -b $1
    fi
fi
